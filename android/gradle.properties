# ---- Gradle JVM (CRITICAL FIX) ----
org.gradle.jvmargs=-Xmx4096m -XX:MaxMetaspaceSize=1024m -Dfile.encoding=UTF-8

# Disable daemon & parallelism (Windows stability)
org.gradle.daemon=false
org.gradle.parallel=false
org.gradle.configureondemand=false

# ---- AndroidX ----
android.useAndroidX=true
android.enableJetifier=true

# ---- React Native ----
reactNativeArchitectures=armeabi-v7a,arm64-v8a,x86,x86_64

# Use ONE value only (disable new architecture for stability)
newArchEnabled=false

# Hermes
hermesEnabled=true

# ---- Signing (unchanged) ----
MYAPP_UPLOAD_STORE_FILE=my-upload-key.keystore
MYAPP_UPLOAD_KEY_ALIAS=key0
MYAPP_UPLOAD_STORE_PASSWORD=xsale9414
MYAPP_UPLOAD_KEY_PASSWORD=xsale9414
